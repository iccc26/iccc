# راهنمای نصب و راه‌اندازی — Iranian Civil Community CIC

نسخه اول سایت: دوزبانه (فارسی/انگلیسی)، چهار بخش اصلی، فرم‌های امن و پنل مدیریت.

---

## ۱. ساختار فایل‌ها

```
index.html              صفحه اصلی سایت (تمام صفحه‌ها داخل همین یک فایل)
assets/logoICCC.png     لوگو
robots.txt , sitemap.xml
.htaccess               اجبار HTTPS و هدرهای امنیتی

api/
  config.sample.php     نمونه تنظیمات — باید کپی و پر شود
  lib.php               رمزنگاری، CSRF، ذخیره‌سازی، محدودیت نرخ، لاگ
  csrf.php              صدور توکن
  submit-support.php    درخواست حمایت (تنها مسیری که فایل می‌پذیرد)
  submit-volunteer.php  ثبت‌نام داوطلب
  submit-partner.php    همکاری با ما
  submit-contact.php    پیام عمومی

admin/
  index.php             پنل مدیریت
```

نیازمندی سرور: **PHP 8.1 یا بالاتر** با افزونه‌های `openssl` و `fileinfo` (روی تقریباً همه هاست‌های اشتراکی فعال است).

---

## ۲. نصب در پنج گام

### گام ۱ — بارگذاری فایل‌ها
محتویات این پوشه را در ریشه وب (`public_html` یا `www`) بارگذاری کنید.

### گام ۲ — ساخت پوشه داده در **بیرون** از ریشه وب
این مهم‌ترین تصمیم امنیتی پروژه است. پرونده‌ها و مدارک نباید هرگز از طریق مرورگر قابل دسترسی باشند.

```
/home/username/
├── public_html/     ← سایت اینجاست
└── iccc-data/       ← پرونده‌ها اینجا، بیرون از دسترس وب
```

```bash
mkdir -p ~/iccc-data
chmod 700 ~/iccc-data
```

### گام ۳ — ساخت کلیدها
روی سرور (یا هر جایی با PHP) اجرا کنید:

```bash
# کلید رمزنگاری
php -r "echo base64_encode(random_bytes(32)), PHP_EOL;"

# هش رمز عبور مدیر
php -r "echo password_hash('یک-رمز-قوی-اینجا', PASSWORD_DEFAULT), PHP_EOL;"
```

> ⚠️ از کلید رمزنگاری یک نسخه پشتیبان آفلاین بگیرید. اگر این کلید گم شود، هیچ پرونده‌ای دیگر قابل خواندن نیست — حتی برای خودتان.

### گام ۴ — ساخت فایل تنظیمات

```bash
cp api/config.sample.php api/config.php
chmod 600 api/config.php
```

سپس در `api/config.php` این‌ها را پر کنید:

| کلید | مقدار |
|---|---|
| `storage_dir` | مسیر کامل پوشه گام ۲، مثلاً `/home/username/iccc-data` |
| `encryption_key` | کلید base64 از گام ۳ |
| `admin_users` | نام کاربری و هش رمز از گام ۳ |
| `notify_email` | ایمیلی که اعلان دریافت درخواست جدید را می‌گیرد |
| `allowed_origins` | دامنه واقعی سایت |

### گام ۵ — گواهی SSL
روی هاست خود Let's Encrypt را فعال کنید. فایل `.htaccess` خودش HTTPS را اجباری می‌کند، اما بدون گواهی معتبر کار نمی‌کند.

---

## ۳. آزمایش پس از نصب

1. `https://دامنه-شما/` باز شود و منوی فارسی/انگلیسی کار کند.
2. یک درخواست آزمایشی از بخش «درخواست حمایت» با یک PDF کوچک بفرستید — باید کد پیگیری بگیرید.
3. وارد `https://دامنه-شما/admin/` شوید و همان درخواست را ببینید و فایل را دانلود کنید.
4. این‌ها **نباید** کار کنند (اگر کار کردند، تنظیمات سرور را اصلاح کنید):
   - `https://دامنه-شما/api/config.php` → خالی یا خطای ۴۰۳
   - دسترسی مستقیم به هر فایلی در `iccc-data`
5. پرونده آزمایشی را از پنل حذف کنید.

---

## ۴. تصمیم‌های امنیتی که در کد اعمال شده

| موضوع | پیاده‌سازی |
|---|---|
| ذخیره پرونده‌ها | JSON رمزگذاری‌شده با **AES-256-GCM** |
| مدارک بارگذاری‌شده | رمزگذاری‌شده، با نام تصادفی و **بدون پسوند** — قابل اجرا یا حدس زدن نیستند |
| محل ذخیره | بیرون از ریشه وب |
| بررسی فایل | هم پسوند و هم MIME واقعی (`finfo`)، سقف ۱۰ مگابایت و ۵ فایل |
| جعل درخواست | توکن CSRF در نشست + بررسی `Origin` |
| ربات‌ها | فیلد تله (honeypot) + محدودیت ۵ ارسال در ساعت برای هر IP |
| ورود مدیر | `password_hash` + قفل ۱۵ دقیقه‌ای پس از ۵ تلاش ناموفق + امکان محدودسازی به IP |
| نشست | کوکی `Secure`، `HttpOnly`، `SameSite=Strict` و بازسازی شناسه |
| داده‌ای که ذخیره **نمی‌شود** | IP، User-Agent و اثر انگشت مرورگر فرد درخواست‌کننده |
| ایمیل اعلان | فقط کد پیگیری را می‌برد، نه محتوای پرونده |
| ردگیری | هر ورود، مشاهده، دانلود و حذف در `iccc-data/logs/audit.log` ثبت می‌شود |

---

## ۵. کارهایی که باید خودتان انجام دهید

**محتوایی** — این موارد با علامت `[...]` در سایت مشخص شده‌اند:
- شماره ثبت شرکت و نشانی دفتر ثبت‌شده (صفحه «ثبت و ساختار» و «راه‌های تماس»)
- آدرس‌های ایمیل واقعی به‌جای `@iraniancivilcommunity.org` (در `index.html` جستجو و جایگزین کنید)
- لینک واقعی شبکه‌های اجتماعی (در `index.html`، هدر و فوتر)

**حقوقی و عملیاتی:**
- ثبت مجموعه به‌عنوان Data Controller نزد **ICO** بریتانیا (هزینه سالانه معمولاً ۴۰ تا ۶۰ پوند)
- نوشتن و انتشار سیاست حفاظت از داده، Safeguarding و شکایات
- تعیین اینکه دقیقاً **چه کسانی** به پنل مدیریت دسترسی دارند — این تصمیم را مکتوب کنید
- پشتیبان‌گیری رمزگذاری‌شده از `iccc-data` و نگهداری کلید در جایی جدا از پشتیبان

---

## ۶. پیشنهاد برای فاز دوم

- بازنشانی خودکار پرونده‌ها پس از دوره نگهداری (`retention_days` در تنظیمات آماده است، فقط یک cron لازم دارد)
- ورود دومرحله‌ای (TOTP) برای پنل مدیریت
- امکان پیگیری وضعیت درخواست توسط خود فرد با کد پیگیری
- انتشار اسناد سیاستی به‌صورت PDF در صفحه «ثبت و ساختار»
- افزودن زبان سوم (عربی یا کردی) — ساختار دیکشنری `I18N` در `index.html` برای این کار آماده است: کافی است یک کلید زبان تازه اضافه کنید
